## 问题
逆序数等运算会不会导致tensor无法自己算梯度？
如果encode之后的k近邻对不上如何进行逆序数计算？
如果能对上能否使用近邻序号之差？
相似矩阵中的阈值是在计算中就采用么

> + 一个衰减因子试一下 最次不加惩罚

第二图是单独的算法？
把相似性和他的特征相乘cat之后使用图算法预测出来的结果是什么样的？

> 先使用三种图神经网络对各种数据进行处理，然后cat起来直接使用到编码器中
> 如果比较可行，最后的解码器约束网络可以换成蛋白和药物的关联

每个数据train一次 实际上一组模型（两个），需要train 6组？
用图神经网络训练的时候标签是什么？

> 把整个autoencoder里面的算法使用GCN GAT GIN
> 另外其实只是叫autoencoder 里面放了两个模型而已 无需把decoder模型反过来和输入做约束
> 相同类别的特征使用同一个模型，不同的特征之间可以不同，交叉进行计算
> 先使用blosun62 把每个蛋白都化成矩阵 然后通过LSTM来化为特征

> 是应该分三个模型 再来一个ae模型
> 还是来个gcn_gat_ae一样的模型

蛋白质干嘛用的？
> 第一个约束是药物蛋白，第二个约束是蛋白疾病
确定整个网络是不是两个gnn_mlp这样的 还是用之前的约束
> 是两个这样的 约束如上

这俩不同维度的tensor能放进去？

## TODO LIST

* ✅ 每对儿模型AUC AUPR
* ✅ 不区分测试集训练集
* 去头方法找效果最好